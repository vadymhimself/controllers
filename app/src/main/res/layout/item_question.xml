<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewModel"
            type="ru.xfit.screens.contacts.faq.QuestionVM" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/content_ll"
        startTransition="@{viewModel.isAnswerVisible}"
        transitionListener="@{viewModel.transitionListener}"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{viewModel::onClick}"
        android:orientation="vertical">

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/fade_color" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/white"
            android:foreground="?attr/selectableItemBackground"
            android:orientation="horizontal"
            android:paddingBottom="15dp"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingTop="15dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="sans-serif"
                android:gravity="center_vertical"
                android:text="@{viewModel.question}"
                android:textColor="@color/slate_grey"
                android:textSize="16sp"
                android:textStyle="normal" />

            <ImageView
                android:id="@+id/expand_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginLeft="16dp"
                android:background="@drawable/ic_keyboard_arrow_down"
                android:tint="@color/slate_grey" />

        </LinearLayout>

        <TextView
            android:id="@+id/expand_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/grayBackground"
            android:clickable="true"
            android:focusable="true"
            android:fontFamily="sans-serif"
            android:lineSpacingExtra="4sp"
            android:paddingBottom="12dp"
            android:paddingLeft="10dp"
            android:paddingRight="10dp"
            android:paddingTop="12dp"
            android:text="@{viewModel.answer}"
            android:textColor="@color/slate_grey"
            android:textSize="12sp"
            android:textStyle="normal" />

    </LinearLayout>

</layout>