<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="controller"
            type="ru.xfit.screens.schedule.ClubClassesController" />

        <import type="ru.xfit.R" />
        <import type="ru.xfit.misc.LayoutManagers" />

    </data>

    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/colorPrimary"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            android:title="@{controller.title}"
            app:menu="@{R.menu.menu_search_filter}"
            app:navigationClickListener="@{controller}"
            app:onCancelListener="@{controller}"
            app:searchListener="@{controller}"
            app:oldItemClickListener="@{controller}"
            app:showBackButton="@{controller.previous != null}" />

        <RelativeLayout
            android:id="@+id/calendar_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/toolbar"
            android:background="@android:color/white"
            android:elevation="4dp">

            <com.molo17.customizablecalendar.library.components.CustomizableCalendar
                android:id="@+id/calendar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:initCalendarSchedule="@{controller}"
                app:layout="@layout/c_calendar"
                app:month_view="false"
                app:multipleSelect="@{false}"
                app:show_subview="false" />
        </RelativeLayout>

        <ru.xfit.misc.views.HackyRecyclerView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/calendar_container"
            android:visibility="@{!controller.adapter.isEmpty()}"
            app:adapter="@{controller.adapter}"
            app:layoutManager="@{LayoutManagers.LINEAR_VERTICAL}" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:text="@string/classes_not_found"
            android:textAlignment="center"
            android:textColor="@android:color/black"
            android:textSize="18sp"
            android:visibility='@{controller.adapter.empty}' />

    </RelativeLayout>
</layout>